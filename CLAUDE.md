# 项目协作规则

- 编码前描述方案并等待用户批准；需求不明确时先澄清。
- 超过三个文件的修改先拆分为可验证的小单元。
- 完成代码后列出潜在问题和测试用例，区分实际执行与待执行。
- 修复 bug 前先编写复现测试，修复到测试通过。
- 用户纠正后在本文件记录防重复规则。

## 2026-09-10 最终技术决策

- 用户最终明确“全用 C#”，当前有效路线为 **Unity + C#**。游戏运行逻辑、编辑器工具与项目测试统一使用 C#。
- 本条取代下方曾讨论的 Unity + Python 路线；不要继续增加 Python 桥接、Python 服务或 Python 项目脚本。
- 基础玩法和 C# 原型方案已获批准，按已约定的小单元继续，不重复询问同一技术选型。
- 项目中不得将没有实际执行的 Unity 编译、Play Mode、真机手柄或构建验证写为通过。
- GitHub 目标仓库此前返回 404；2026-09-10 用户提供链接后已确认可访问，初始核心代码已写入开发分支。仍须核对每次远程写入结果后才能报告已提交。

## 2026-09-10 历史调整：Unity + Python（已被最终决策取代）

- 用户曾要求 **Unity + Python**，随后在了解支持边界后最终改回全 C#。
- 当时曾暂停扩展 C# 草稿以澄清 Python 的职责；该暂停已随最终 C# 决策解除。
- 不把 Unity 的 Python Scripting 编辑器包当作发布后可运行的游戏脚本方案；所选 Unity 版本的支持状况要以官方文档核实。
- 历史讨论明确了“混合语言方案必须说明引擎侧与桥接职责”的原则，当前项目没有采用该混合方案。
- 该讨论期间只产生三个初步 Runtime 草稿，尚未形成运行闭环；当前进度以 README 和导入说明为准。
